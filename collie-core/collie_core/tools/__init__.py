"""Collie life tools.

Tools in this package follow the engine ``Tool`` contract and are loaded at
runtime with ``ToolLoader(collie_core.tools)`` in addition to the built-in
engine tools (web search/fetch, message, cron, image generation, MCP).
"""
