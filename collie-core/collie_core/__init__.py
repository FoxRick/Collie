"""Collie core package.

Collie-specific code lives here: SQLite storage, structured profile memory,
IPC protocol for the Electron shell, OAuth providers, and the 16 life tools.

The agent engine (loop, runner, providers, tool registry, MCP client,
WebSocket transport) is the stripped nanobot fork in the ``nanobot`` package.
"""

__version__ = "0.1.0"
